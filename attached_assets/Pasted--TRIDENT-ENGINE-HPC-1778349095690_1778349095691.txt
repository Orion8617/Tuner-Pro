// ═══════════════════════════════════════════════════════════════════════════ // TRIDENT ENGINE HPC — Corrected & Optimized Core // ═══════════════════════════════════════════════════════════════════════════ const DEG2RAD = Math.PI / 180; const M_PER_DEG = 111320.0; // ── Vigesimal Quantization ──────────────────────────────────────────────── export const VIGESIMAL_SYMBOLS = [ '◎','•','••','•••','••••', '━','━•','━••','━•••','━••••', '━━','━━•','━━••','━━•••','━━••••', '━━━','━━━•','━━━••','━━━•••','━━━••••', ]; export function quantVigesimal(value, min_v, max_v) { if (max_v <= min_v) return 10; const norm = Math.max(0.0, Math.min(1.0, (value - min_v) / (max_v - min_v))); return (norm * 20) | 0; } export function dequantVigesimal(level, min_v, max_v) { const center = (level + 0.5) / 20.0; return min_v + center * (max_v - min_v); } export function vigesimalSymbol(level) { return VIGESIMAL_SYMBOLS[level < 0 ? 0 : (level > 19 ? 19 : level)]; } // ── RF Physics ──────────────────────────────────────────────────────────── // FSPL con unidades seguras (km + MHz) export function fspl(d_m, f_hz) { if (d_m <= 0 || f_hz <= 0) return 0; const d_km = d_m * 1e-3; const f_mhz = f_hz * 1e-6; return 32.44 + 20 * Math.log10(d_km) + 20 * Math.log10(f_mhz); } export function cost231Hata(d_km, f_mhz, h_bs = 30, h_ms = 1.5) { if (d_km < 0.02) d_km = 0.02; const f_log = Math.log10(f_mhz); const a_hm = (1.1 * f_log - 0.7) * h_ms - (1.56 * f_log - 0.8); return 46.3 + 33.9 * f_log - 13.82 * Math.log10(h_bs) - a_hm + (44.9 - 6.55 * Math.log10(h_bs)) * Math.log10(d_km); } export function timingAdvanceToDistance(ta) { return Math.max(0, ta * 78.125); } export function rssiToDistance(rssi, tx_power = -30, freq_ghz = 2.4) { const path_loss = tx_power - rssi; const n = freq_ghz > 3.0 ? 3.0 : 2.7; return Math.max(1.0, Math.pow(10, path_loss / (10 * n))); } export function rsrpFromRssi(rssi, n_rb = 50) { if (rssi <= -120) return -140; return rssi - 10 * Math.log10(12 * n_rb); } export function correctTaForAltitude(d_slant, alt_m) { if (alt_m <= 0 || d_slant <= alt_m) return d_slant; return Math.sqrt(d_slant * d_slant - alt_m * alt_m); } // ── Spatial Optimization ─────────────────────────────────────────────────── // Hash sin strings (cache-friendly) export function dedupeTowersHPC(towers) { const seen = new Set(); const result = []; for (let i = 0; i < towers.length; i++) { const t = towers[i]; const key = ((t.lat * 10000) | 0) * 100000 + ((t.lng * 10000) | 0); if (!seen.has(key)) { seen.add(key); result.push(t); } } return result; } // GDOP con coordenadas en metros (FIX CRÍTICO) export function computeGDOP(anchors, target_lat, target_lon) { const n = anchors.length; if (n < 3) re`	turn 99.0; let h00 = 0.0, h01 = 0.0, h11 = 0.0; const cosLat = Math.cos(target_lat * DEG2RAD); for (let i = 0; i < n; i++) { const dx = (anchors[i].lon - target_lon) * M_PER_DEG * cosLat; const dy = (anchors[i].lat - target_lat) * M_PER_DEG; let r = Math.sqrt(dx * dx + dy * dy); if (r < 1e-6) r = 1e-6; const hx = dx / r; const hy = dy / r; h00 += hx * hx; h01 += hx * hy; h11 += hy * hy; } const det = h00 * h11 - h01 * h01; if (Math.abs(det) < 1e-12) return 99.0; const trace_inv = (h11 + h00) / det; return trace_inv > 0 ? Math.min(Math.sqrt(trace_inv), 99.0) : 99.0; } export function gdopRating(score) { if (score < 2) return 'Excellent'; if (score < 5) return 'Good'; if (score < 10) return 'Poor'; return 'Critical'; } // ── HPC Gauss-Newton IRLS Solver (Corregido) ─────────────────────────────── export function solveGN_IRLS_HPC(startX, startY, circles, maxIter = 40) { let sx = startX, sy = startY; let lam = 5.0; let nu = 2.0; const N = circles.length; let costOld = Infinity; for (let iter = 0; iter < maxIter; iter++) { let j00 = 0.0, j01 = 0.0, j11 = 0.0; let jr0 = 0.0, jr1 = 0.0; let currentCost = 0.0; for (let i = 0; i < N; i++) { const c = circles[i]; const dx = sx - c.x; const dy = sy - c.y; if (!Number.isFinite(dx) || !Number.isFinite(dy)) continue; let d = Math.sqrt(dx * dx + dy * dy); if (d < 1.0) d = 1.0; const residual = d - c.r; // ── Tukey IRLS (real) ── const sigma = 10.0; const u = residual / sigma; const w = Math.abs(u) < 1 ? Math.pow(1 - u * u, 2) : 0; currentCost += w * residual * residual; const invD = 1.0 / d; const jx = dx * invD; const jy = dy * invD; j00 += w * jx * jx; j01 += w * jx * jy; j11 += w * jy * jy; jr0 += w * jx * residual; jr1 += w * jy * residual; } // LM damping (más correcto) j00 += lam; j11 += lam; const det = j00 * j11 - j01 * j01; if (Math.abs(det) < 1e-12) break; const stepX = (jr0 * j11 - jr1 * j01) / det; const stepY = (j00 * jr1 - j01 * jr0) / det; const newX = sx - stepX; const newY = sy - stepY; let costNew = 0.0; for (let i = 0; i < N; i++) { const c = circles[i]; const dx = newX - c.x; const dy = newY - c.y; let d = Math.sqrt(dx * dx + dy * dy); if (d < 1.0) d = 1.0; const res = d - c.r; const sigma = 10.0; const u = res / sigma; const w = Math.abs(u) < 1 ? Math.pow(1 - u * u, 2) : 0; costNew += w * res * res; } // FIX: comparar contra currentCost if (costNew < currentCost) { sx = newX; sy = newY; costOld = costNew; lam = Math.max(lam * 0.33, 1e-7); nu = 2.0; } else { lam *= nu; nu *= 2.0; if (lam > 1e6) break; continue; } if (Math.abs(stepX) < 0.005 && Math.abs(stepY) < 0.005) break; } return { x: sx, y: sy, cost: costOld }; } // ── Orchestrator ─────────────────────────────────────────────────────────── export async function processTridentRequest(body) { return { ok: true, data: { status: "Trident HPC Optimized Core Ready" } }; }